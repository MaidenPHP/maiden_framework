write tests for this 'ORM'
configure phpunit

figure ou the basic CRUD wrapper for the SQL

`````````````````````````
use Maiden/DB as DB

class AuthController
{
    function __construct()
    {
        $this->db = new DB();
    }

    function test()
    {
        raw sql

        $this->db->query("
            SELECT *
            FROM table
            WHERE
        ");

        $this->db->create('table', [
            'param1' => 'value1'
        ]);
    }
}